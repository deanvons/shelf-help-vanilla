// we can define URLs here and reference them in code
// this way if we need to change them we can do it here once
// instead of going through and updating wherever it is used like API calls or page redirects
export const HOST_URL = "http://127.0.0.1:5500"
export const API_URL = "http://localhost:3000"